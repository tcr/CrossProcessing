package processing.api {
	import processing.api.*;

	public class AniSprite {
/*[TODO]
		public var images:Array = [];
		public var pos:Number = 0;
		
		private var context:Context;
		
		private function nf( num, pad )
		{
			var str = '' + num;
			while ( pad - str.length )
				str = '0' + str;
			return str;
		}
		
		public function AniSprite(p, prefix, frames ) {
			_context = p;
			
			// add frames
			for ( var i = 0; i < frames; i++ )
			{
				images.push( prefix + p.nf( i, ("" + frames).length ) + ".gif" );
			}
		}
		
		public function display( x, y )
		{
			context.image( this.images[ this.pos ], x, y );
			if ( ++this.pos >= frames )
				this.pos = 0;
		}
		
		public function getWidth():Number
		{
			return context.getImage(this.images[0]).width;
		}
		
		public function getHeight():Number
		{
			return context.getImage(this.images[0]).height;
		}*/
	}
}
